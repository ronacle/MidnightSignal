# Midnight Signal v14.1 - Conversion Layer

Added data-driven Pro CTA, locked analytics preview, Pro unlocked state, and clearer free-vs-Pro positioning.

Deploy with `npm install`, `npm run build`, then `vercel --prod`.
